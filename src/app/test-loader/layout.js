export const metadata = {
    title: "Test Loader - Vicophar",
    robots: {
        index: false,
        follow: false,
    },
};

export default function TestLoaderLayout({ children }) {
    return <>{children}</>;
}
